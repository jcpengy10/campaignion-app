<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Campaignion</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="wkfhtml" type="text" class="ghost-input" placeholder="Paste workflow html here..." required>
          <input type="submit" class="ghost-button" value="Process">
        </form>
        <!-- Replace html with svg diagram -->
        <% if(html!== null){ %>
            <svg id="labeled-workflow"><li>random stuff</li></svg>
            <p><%= html %></p>
          <% } %>
        <!-- ***** -->
          <% if(error !== null){ %>
            <p><%= error %></p>
          <% } %>
      </fieldset>      
    </div>
  </body>
</html>